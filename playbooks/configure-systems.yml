# Playbook to call multiple roles to configure systems
- name: Configure systems
  hosts: 10.20.128.243
  become: yes

  tasks:

- name: Call Roles
  hosts: servers
  roles:
   #- hostname
   #- firewalld
   #- ntp-config
   - motd
